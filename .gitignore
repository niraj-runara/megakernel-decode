# upstream, cloned by scripts/00_provision.sh
reference-megakernels/

# results/ IS tracked — the logs are the provenance for RESULTS.md, and the
# instance they were produced on is ephemeral. They are ~100K of plain text.

# python envs created by scripts/00_provision.sh
.venvs/
__pycache__/
*.py[cod]
.python-version

# build artifacts
*.so
*.o

# model weights / caches
*.safetensors
*.bin
.cache/
hf_cache/

# os
.DS_Store

# local editor/tool config
.claude/
